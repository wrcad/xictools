#! /bin/sh
#
# This is where the release numbers are set.
#

release="4"
tools_version="4.3"
vl_version="4.3.1"

if [ z$1 != "z" ]; then
    case $1 in
    release)
        echo $release
        ;;
    tools)
        echo $tools_version
        ;;
    vl)
        echo $vl_version
        ;;
    esac
fi

