
# This is an example script which converts all boxes on layer CAA that
# cover CPG to CCP.  This is for use with the scmos tech file, as an
# example of how to repair a file generated by another CAD system.

i = InitGen()
while ((name = GenCells(i)) != 0)
    Edit(name, 0)
    TextCmd("select l CCA b Overlap CPG")
    SetCurLayer("CCP")
    ChangeLayer()
    Commit()
end